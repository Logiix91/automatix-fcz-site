---
target: index.html
total_score: 23
p0_count: 1
p1_count: 2
timestamp: 2026-06-30T06-27-49Z
slug: index-html
---
Method: dual-agent (A: a577b245a62c06978 · B: a7f079281b7736ddc)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Reveal animations and hover states present. Form submit shows a message — but that message admits the form is non-functional, which undermines rather than builds confidence. |
| 2 | Match System / Real World | 3 | Sourced citations (CBS, MIT) match the target audience's evaluation frame. The dark-mode AI-startup visual register does not match "blunt, technical, Dutch logistics/government" mental models. |
| 3 | User Control and Freedom | 2 | html lang="en" but runtime default is NL — a control mismatch. Mobile menu has no explicit close button. No back-to-top on a long page. |
| 4 | Consistency and Standards | 2 | Internally consistent system, but built on standard AI-SaaS grammar rather than a distinctive brand standard. Service numbering skips 8 (services.7 → services.9) — a structural artifact visible in footer links. |
| 5 | Error Prevention | 2 | required/type=email on form fields. No inline field validation. The form's submit path is a trust failure, not a safety net. |
| 6 | Recognition Rather Than Recall | 3 | Persistent nav, anchor links, footer mirrors nav. Six-phase names are self-contained. |
| 7 | Flexibility and Efficiency of Use | 2 | No skip-to-content, no keyboard shortcuts, no CTA in the mobile menu. Single fixed scroll path. |
| 8 | Aesthetic and Minimalist Design | 2 | Multiple simultaneous noise sources in hero: grid texture + ember glow blobs + floating cards + gradient text + canvas animation. Canvas renders as blank white bars if JS is slow. |
| 9 | Error Recovery | 1 | No inline validation errors. No alternative contact path surfaced when the form submit reveals it is non-functional. |
| 10 | Help and Documentation | 3 | Pricing model explained clearly. About/Honesty sections pre-answer legitimacy questions. Cookie consent explains analytics approach. No FAQ, no SLA transparency. |
| **Total** | | **23/40** | **Acceptable — significant improvements needed** |

## Anti-Patterns Verdict

**Does this look AI-generated?**

**LLM assessment (Assessment A)**: Partial — leaning yes. The copy voice is genuinely disciplined and differentiating; the visual mechanics are built almost entirely from patterns the project's own brief lists as anti-references. Specific tells:

- **Gradient text on hero.title2** (`index.html:66–69`, used line 130): `.gradient-text { background-clip: text; -webkit-background-clip: text }` applies an orange gradient to "We make automation actually work." PRODUCT.md names "gradient hero text" as the explicit anti-reference. The site executes it anyway, in the largest type on the page, in the first viewport.
- **Hero-metric-card template** (lines 141–157): floating "Workflow: Lead intake" card + "312h" hours-saved card with progress bar — both named directly in PRODUCT.md's anti-references list as the hero-metric-card template.
- **Eyebrow label on every section** (lines 125, 165, 189, 271, 316, 363, 375, 462 — 8 instances): identical `text-xs font-semibold uppercase tracking-wide` pattern on every section. The brand register ban: "Repeated tiny uppercase tracked labels above every section heading. A single strong kicker can be voice; repeating it as section grammar is AI scaffolding."
- **Numbered 01–06 section markers** (HOW IT WORKS, lines 279–304): ghost numerals as default scaffolding, not earned by the content being a true sequence requiring order.
- **Identical card grids**: 8 service cards (lines 196–261), each with identical icon-chip + title + 2-line desc + "Learn more →"; 6 HOW IT WORKS cards all identical in visual weight.
- **Inter as the sole typeface** (line 40 import, line 46 config): explicitly first on the brand register's reflex-reject list. No typographic POV.
- **Zero imagery** (the About section "image" at lines 411–420 is a gradient div + blurred blob + text overlay): brand.md rule — "Zero imagery on a brief that implies imagery. Colored blocks where a hero photo belongs."
- **First-order slop test**: navy + orange on dark + Inter + grid overlay = the modal AI startup landing page of 2024–2025. The palette does not read as a named reference tied to the brand's Rotterdam/Dutch identity.
- **Second-order slop test**: even knowing the brief's anti-references, the page lands squarely in the Stripe-adjacent dark SaaS aesthetic lane.
- What saves it from a flat "yes": the copy itself is uncommonly specific and honest — sourced CBS/MIT statistics, a named 6-phase methodology with procedural detail, an Honesty section that states team size and no investors. The writing resists the genre; the visuals embrace it.

**Deterministic scan (Assessment B)**: Exit code 2 on both homepage and full-site scans. Findings:

| Rule | Count | Scope | Key Location |
|------|-------|-------|-------------|
| `overused-font` (Inter) | 22 hits | All 11 pages | index.html:40, 58; every other page:17, 35 |
| `single-font` | 11 hits | All 11 pages | Every page (one per file) |
| `side-tab` | 1 hit | Homepage only | index.html:377 (`border-l-4 border-ember-500`) |
| `marketing-buzzword` | 1 hit | Homepage only | index.html (traces to line 251, "Enterprise-grade voice and chat") |
| `numbered-section-markers` | 1 hit | Homepage only | index.html (sequence 01–06, lines 279–304) |

The detector surfaced the `side-tab` `border-l-4` accent on the About section pull-quote (line 377) — a named absolute-ban pattern that the design review did not independently flag as a priority issue. Both assessments independently converged on `marketing-buzzword` (Assessment A: P3 issue, specific line 251; Assessment B: exact same hit). The detector did not produce a `gradient-text` finding despite that being an explicit named absolute ban — the LLM review caught it directly by reading the CSS; this appears to be a gap in the current detector's pattern coverage rather than a false positive.

No false positives in the detector output. All hits trace to live first-party authored code.

**Visual overlays**: browser visualization was unavailable — no browser automation tool was exposed in either assessment's environment.

## Overall Impression

The copy is the strongest, most differentiated thing on this site. The visual system around that copy looks like the exact templated AI-consultancy site the words are trying to distance the brand from. The brief explicitly names its own anti-references; the homepage ships almost all of them. The biggest single opportunity: give the visual layer the same specificity and honesty the copy already has.

## What's Working

1. **Sourced, inline citations in the VALUE GAP section** (lines 169–181): "1 in 6 Dutch businesses... (CBS)", "~95%... (MIT)" — not vague percentages, actual named sources in the same sentence as the claim. This is rare in this category and is exactly the "show, don't hype" principle executed correctly.
2. **The HOW IT WORKS methodology copy** (lines 280–307): specific procedural commitments ("we physically visit... shadow a full process... online surveys alone are deliberately not enough here," "No measurable result means going back to the drawing board") that are falsifiable and differentiate from competitor boilerplate.
3. **The Honesty section** (lines 360–368): "two founders, no outside investors, we'd rather grow slowly on proven results than market with claims we can't back up yet" — genuinely bold, on-brief, correctly positioned before the CTA. Most consultancies this size inflate; this one explicitly refuses to.

## Priority Issues

**[P0] Demo form tells visitors their submission went nowhere — the site's core conversion path is non-functional**
- What: form (lines 471–479) collects real name/company/email/message, then on submit reveals: "Thanks! This is a demo form. Connect it to your CRM or email service to go live." No data is transmitted. Compounded by: footer shows KVK: 00000000 (line 524) and a sequential placeholder phone number +31101234567 (line 467) — both checkable facts a Dutch B2B/government buyer will verify.
- Why it matters: A skeptical B2B buyer who fills out a specific question about their logistics process and gets told it went nowhere has just experienced the precise opposite of what the site promises ("real engineering rigor," "honest about limitations"). The KVK placeholder adds a procurement blocker for Dutch government buyers for whom a real, searchable KVK number is how they verify a vendor is real.
- Fix: wire the form to a real endpoint (Formspree, mailto fallback, or the CRM referenced in the pricing copy) and replace placeholder KVK/phone before this page is treated as live. Remove the demo-form disclaimer from both i18n dictionaries.
- Suggested command: `/impeccable harden`

**[P1] Gradient text on hero.title2 directly contradicts the project's own anti-references list**
- What: `.gradient-text` (defined lines 66–69, applied line 130). PRODUCT.md names "gradient hero text" as the explicit anti-reference. The hero ships it anyway in the largest type on the page.
- Why it matters: the first viewport is the most critical moment for a hype-fatigued B2B buyer, and it opens with the single visual pattern the brief says this site should never look like.
- Fix: remove `.gradient-text` from `hero.title2`. Use `text-ember-400` solid color for emphasis, or run the headline monochrome white with weight contrast only. The copy does not need a gradient; the plainspokenness is already the hook.
- Suggested command: `/impeccable quieter`

**[P1] Eight section eyebrows in identical pattern — a named absolute ban, shipped on every section**
- What: 8 instances (lines 125, 165, 189, 271, 316, 363, 375, 462) all `text-xs font-semibold uppercase tracking-wide`. Indistinguishable from each other, making them invisible rather than orienting.
- Why it matters: brand register ban — "repeating it as section grammar is AI scaffolding." This is the single most recognizable AI-template pattern on the page, applied to every section without exception.
- Fix: remove eyebrows from 6 of 8 sections; let headlines carry the navigation burden. Keep only where a register shift genuinely needs signalling — the HONESTY "Where we stand" eyebrow earns its place. Optionally, keep "Het Automatix Framework" / "The Automatix Framework" as the named system brand for the HOW IT WORKS section.
- Suggested command: `/impeccable distill`

**[P2] Inter as the sole typeface, chosen by reflex, confirmed site-wide by detector (22 overused-font + 11 single-font hits across all 11 pages)**
- What: single font-family `Inter` at weights 400–800, configured globally (line 46), loaded (line 40), used for every element from display headlines to body copy to labels.
- Why it matters: Inter is first on the brand register's reflex-reject list. The site has no typographic POV — if the palette were changed, nothing about the type would retain the brand's character. For a brand claiming "technical, blunt" personality for a Rotterdam industrial/logistics audience, Inter reads as generic tech-startup neutral.
- Fix: run the font-selection procedure from brand.md — three voice words (e.g. "blunt, procedural, Dutch-civic"), find a headline face with structural character in that register. Pair a personality-driven display/heading font with Inter (or another refined body face) rather than using Inter for everything.
- Suggested command: `/impeccable typeset`

**[P2] About section image is a gradient div where a Rotterdam photograph should be**
- What: lines 411–420, `bg-gradient-to-br from-navy-900 to-navy-700` with blurred ember blob and "Headquarters / Rotterdam, Netherlands" text overlay. No actual photograph.
- Why it matters: brand.md rule — "Zero imagery on a brief that implies imagery. Colored blocks where a hero photo belongs." A consultancy with "Built in Rotterdam. Built for European business." as an About headline and a specific street address (Wilhelminakade 12) has Rotterdam as a brand anchor — and cannot show it. For a skeptical buyer, a photograph of an actual address is trust evidence; a gradient blob is a placeholder they can see through.
- Fix: ship one photograph. Rotterdam Wilhelminakade is visually distinctive. A single verified Unsplash image of the Rotterdam waterfront (verify the URL resolves before deploying) or a real founders photo does more trust-building work than any stat card.
- Suggested command: `/impeccable delight`

**[P3] "Enterprise-grade" buzzword breaks the page's anti-hype voice; border-l-4 side-tab is a named absolute ban**
- What: (a) `services.7.desc` (index.html line 251): "Enterprise-grade voice and chat interfaces" — a literal banned buzzword sitting inside a brand that elsewhere explicitly disavows ungrounded superlatives; (b) `index.html:377` `border-l-4 border-ember-500 pl-4` on the About pull-quote — a `border-left: 4px` colored accent, the named "side-tab" absolute ban surfaced by the detector and missed in the manual pass.
- Fix: (a) replace with a specific, falsifiable claim: "voice and chat interfaces that hold context across WhatsApp, web chat and phone, in the languages your customers use"; (b) replace `border-l-4` with either a full border (`border border-ember-500/40`), a background tint, or remove the accent entirely.
- Suggested command: `/impeccable clarify` for (a); `/impeccable polish` for (b)

## Persona Red Flags

**Jordan (Confused First-Timer)**: Arrives, sees the HERO with six competing focal points (badge + two-line headline + subtitle + two CTAs + two floating cards + canvas animation). The floating cards (lines 141–157) show a mock workflow UI — "New lead detected / AI agent qualifies lead / CRM updated automatically" — but Jordan cannot tell if this is software the company sells, a process they implement, or decoration. The canvas animation renders as a blank white bar at SERVICES (lines 193–195) and HOW IT WORKS (lines 274–276) if JS hasn't loaded. Jordan then faces 8 undifferentiated service cards with no "start here" cue or filter.

**Riley (Deliberate Stress Tester)**: Submits the contact form, reads "Thanks! This is a demo form." (line 479) — the most damaging thing a stress-tester could find, and it is the form's by-design success state. Checks the footer: KVK 00000000 (line 524). Checks source code: services.8 is missing from the i18n key sequence (jumps services.7 → services.9). Checks "Book a free call" in mobile nav: it is not present in the mobile menu (lines 110–116), only in the desktop nav. Three red flags found in 90 seconds of deliberate testing.

**The Skeptical Dutch Government IT Procurement Lead** (AVG-sensitive, has seen three failed GenAI pilots): First checkpoint: KVK number — finds 00000000, a hard procurement stop. Second: AVG posture — finds cookie consent reopen in footer (good), but the hero subtitle buries "always GDPR-first by design" as the last clause of a 60-word sentence. Third: evidence of past work — zero case studies, zero named clients, the "312h saved" hero metric is unsourced. Fourth: founding date 2026 with placeholder legal identifiers — Dutch government procurement typically requires 12+ months of trading history with auditable accounts. This persona is disqualified before reaching the contact form, for reasons the site could fix.

## Minor Observations

- `html lang="en"` (line 2) but i18n.js defaults runtime language to `nl` — metadata/runtime mismatch affecting screen readers and SEO language signaling.
- The `floaty` animation on the hero cards (lines 72–74) has no `prefers-reduced-motion` guard in the main `<style>` block, even though `consent.js` correctly checks this pattern elsewhere.
- `trust.label` key exists in i18n.js ("Trusted by logistics, industry and public-sector teams across the Benelux") but is not rendered anywhere on the homepage — an unused social-proof claim that could have value on the page.
- Service numbering skips 8 in i18n.js and footer (services.7 → services.9) — a code artifact but also a structural inconsistency visible to anyone reading source.
- The logo "FCZ" suffix in the navbar (`Automatix FCZ`, line 88) is never explained anywhere on the page. Not knowing what "FCZ" stands for creates a minor unresolved question mark for a first-time visitor.
- The detector's `marketing-buzzword` snippet shows "sational AI Enterprise-grade" — this is a scanner rendering artifact (the word "Conversational" split across a line boundary). The actual match is "Enterprise-grade" at line 251, which is a real hit, not a false positive.

## Questions to Consider

1. **The brief names specific anti-references — gradient text, hero-metric cards, identical card grids — and the homepage ships all three in the first two sections. Was the brief written before or after the homepage, and has it ever been reconciled against what shipped?**

2. **The Honesty section is the most differentiated writing on the page, and it is presented in the same visual register as the pricing cards. If "honest and blunt" is the brand's actual competitive advantage, what would it look like if the Honesty section broke every other design rule on the page to signal: this part is different from every other AI consultancy site you've seen?**

3. **The hero shows a mock "312 hours saved" metric for a company founded in 2026 with no published clients. If the site's central message is "close the gap between AI hype and AI value," is the hero's own hype-metrics-before-evidence pattern consistent with that message — or should the hero instead lean fully into the "we're new, here's exactly what that means for you" posture that the Honesty section already takes?**
